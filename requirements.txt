# SLIM / cloud-safe requirements — used by Streamlit Community Cloud.
#
# The free tier (~1GB RAM) cannot run torch + transformers + Qwen, so this file
# deliberately omits them. The app degrades gracefully: it serves the committed
# TF-IDF + churn models, rule-based outreach, regex/keyword entities, and the
# pre-computed evaluation metrics/figures — every tab works.
#
# For the FULL local experience (DistilBERT, translation, Qwen, spaCy), use:
#     pip install -r requirements-full.txt

streamlit>=1.40
pandas>=2.0
numpy>=1.26
scikit-learn==1.6.1   # pinned to match the committed .joblib models
joblib>=1.3
plotly>=5.20
langdetect
